const CheckoutProduct = ({
	id,
	name,
	price,
	description = "",
	category,
	image,
	average_rating,
	ratings_count,
	hasPrime,
}) => {
	return (
		<div>
			<h1>{product.id}</h1>
		</div>
	);
};

export default CheckoutProduct;
